services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./sprint_quiz.db:/app/sprint_quiz.db
      - ./data/sample:/app/data/sample
    restart: unless-stopped
